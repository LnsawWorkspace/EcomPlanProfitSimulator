# AIREADME.md — 给 AI 代理的网站使用说明书

> 本文档专门写给 AI 代理（LLM Agent）阅读，帮助其快速、正确地使用本网站。
> 人类用户请阅读网站内操作指引。
> 最后更新：2026-08-19（由对网站源码和业务语义进行过深度调研的 AI 编写）

---

## 1. 一句话定位

**全场景单品推演**：一个电商单品（单个 SKU、单链接单规格）的利润沙盘推演工具。输入售价、目标单量、退款率、推广 ROI、商品成本、赠品、各类支出，输出完整利润报告与敏感性分析。

- 纯前端 SPA，**无后端**，数据存储在浏览器 IndexedDB
- 页面框架：Bootstrap 5 + 原生 ESM 模块；图表：ECharts（treemap）+ D3（瀑布图）
- 计算引擎：`SimulationCore.runSimulation`（Decimal 高精度，单次 ~100ms）

## 2. 页面导航

| 页面 | URL | 说明 |
|------|-----|------|
| 首页 | `/` | 产品介绍 |
| 工作台 | `/page/workbench/workbench.html` | 方案组/方案管理 |
| 推演参数 | `/page/planParams/planParams.html?workspaceId=&groupId=&planId=` | 填参数（新标签打开） |
| 利润报告 | `/page/planReport/planReport.html?workspaceId=&groupId=&planId=` | 查看报告 |
| 6 个敏感性分析图 | `/page/planReport/planReport{Roi,Sale,Volume,SaleVolume,RoiSale,RoiVolume}Graph.html?...` | 单/双变量扫描（重计算） |

**重要**：推演参数页由工作台"调整方案"按钮 `window.open` 打开**新标签**，AI 操作时需监听新页面事件。

## 3. 核心概念

### 3.1 三级数据模型

```
工作区 (Workspace) → 方案组 (Plan Group) → 方案 (Plan)
```

- **工作区**：顶层隔离空间（数据空间面板管理，可建多个）
- **方案组**：一个商品 SKU 的归类
- **方案**：一次具体推演（含全部参数）

### 3.2 一个方案 = 一个逻辑渠道（最重要的架构哲学）

"渠道"不限于现实渠道。同一链接的**自然流量、推广成交、满减活动、不同人群（花呗/银行卡）**，每个都是独立的逻辑渠道 → **各建一个方案**。复杂场景（阶梯满减、满赠、多平台）用"多方案"表达，不增加字段。

### 3.3 推演参数 6 大模块

目标（售价/单量/退款率）· 销售金额分摊（成本/公允）· 推广（ROI）· 商品信息 · 赠品信息 · 支出（每单/部分订单/固定）

## 4. 关键参数语义（AI 必读，防踩坑）

| 参数 | 正确语义 | 易错点 |
|------|---------|--------|
| **售价** | **成交一单的销售价格**（单单价） | 不是单件价 |
| **商品件数** | **这一单里包含几个该商品**（每单件数，默认 1）| 只影响**成本侧**：收入=售价×单量不变，成本=件数×单量×单价。售价 199 件数 3 = 一单 199 元含 3 件货 |
| 采购金额/数量 | 不知道单价时用；会**覆盖**含税/不含税成本 | 与成本二选一 |
| 含税/不含税成本 | 二选一自动互算（不含税 = 含税/(1+进项税率)） | — |
| **ROI** | **= GMV(退款前销售额 = 售价×目标单量) ÷ 广告成本** | 不是"广告费/销售额"！单广告费 = 售价 ÷ ROI |
| 退款率 | 订单退款比例（售前/售中/售后三阶段） | 输入百分数，内部自动 /100 |
| **回收率** | 退款后成本能收回的比例（默认：商品/赠品/广告 100%，每单支出 0%） | 100% = 零成本损失；0% = 全损 |
| 公允价值 | **不含税**（税法规定）；多商品且销项税率不一致时才需要 | 公允分摊法用 |
| 分摊法 | 商品 > 1 且销项税率不一致 → **公允分摊法** | 单一税率用成本分摊 |
| 支出"基于" | 每单/部分订单只有"售价"；固定支出支持 付款金额/销售金额/收入/利润 | — |
| 部分订单支出"退款率" | **独立于总退款率**，针对该支出子集的退款率（如 10% 订单中 50% 退款） | 与目标模块总退款率无关 |

## 5. 计算口径（利润报告如何得出）

- 收入 = 售价 ÷ (1 + 销项税率)（不含税）
- 单广告费 = 售价 ÷ ROI ÷ (1 + 广告税率)
- **有效成本 = 退款后成本 + 总退款损失**（损失部分 × (1-回收率)）
- 总成本 = 商品 + 赠品 + 每单支出 + 部分订单支出 + 固定支出 + 广告 的有效成本之和
- **利润 = 收入(退款后) − 总成本**
- 利润率 = 利润 ÷ 收入；资本回报率 = 利润 ÷ 总成本；推广回报率 = 利润 ÷ 广告有效成本
- "因退款造成的利润损失" = 0 退款理想利润 − 实际利润
- **价税分离**：主视图（指标/瀑布图/treemap）全为不含税口径，税细节只在明细表显示（如赠品"额外缴税"列）

### 赠品视同销售（财税）

- 法规：《增值税暂行条例实施细则》第四条第八项——无偿赠送视同销售
- 实现：应税金额 = (公允价值 − 不含税成本) × 数量（公允=0 时按成本×10%）；额外缴税 = 应税金额 × 销项税率
- 该"净税负"口径与标准税法等价（销项−已抵扣进项），已确认设计正确

## 6. 报告页解读

- **13 个指标卡**：付款单量/有效单量/付款金额(GMV)/销售额(退款后)/收入(退款后)/广告费/总成本/利润/推广回报率/资本回报率/利润率/成本损失(退款)/利润损失(退款)。负值红字、正值蓝字
- **收入-利润瀑布图**（D3）：收入 → 商品 → 赠品 → 广告 → 费用 → 利润
- **成本结构 treemap**：各项成本占总成本比例（一眼找最大成本项）
- **退款损失结构 treemap**：退款造成各项成本损失占比（反直觉：广告常是退款损失大头，因商品回收率高而广告售后回收低）
- **4 个明细表**：收入明细（**旧版"多渠道方案"残留，tbody 为空，已废弃**）、商品成本明细、赠品成本明细（含额外缴税列）、运营成本明细。明细表作用：定位损失来源（哪个商品/赠品/支出、售前/售中/售后哪阶段）、辅助决策
- **6 个敏感性分析图**：单变量（ROI/售价/单量）≈3s；二维（售价×单量/ROI×售价/ROI×单量）≈20-40s 重计算，热力图找最优组合

## 7. 性能与调试

- 全静态站，本地缓存后速度快；但**分析图（尤其二维）计算慢**，需耐心等待
- 控制台时间戳：`+ start Simulation: Xms` / `+ end Simulation: Xms`（耗时=差值）；`计算完成，结果：[...]`
- **可读性报表**：打开报告页时控制台打印完整 JSON（约 48KB，8 个报告模块全部中间值），AI 可用 `page.on('console')` + `m.args()` 提取做深度分析

## 8. 操作流程（AI 代理标准流程）

1. 打开工作台，检查/切换目标工作区（数据空间面板）
2. 进入方案组（点击卡片；不存在则新建，**名称不能与已有重复**）
3. 新建方案或进入已有方案（方案名称同样不能重复）
4. 点"调整方案"（sliders 图标）→ 捕获新标签 → 填参数
5. "保存方案" 或 "保存并查看报告"（后者打开报告新标签）

**方案/方案组创建后持久保留，不能重名。重复创建会失败。已存在的直接进入使用。**

### 参数录入方法论（AI 必读）

**帮用户录入参数时，AI 应主动遍历所有可能的费用/支出/确认点，逐项询问用户**——人容易漏项，AI 对各大平台支出熟悉，应充当"支出清单引擎"。

- 遍历范围：平台类（佣金/技术费/手续费）、支付类（花呗/信用卡）、物流类（运费/逆向/运费险/包装）、推广类（ROI/坑位费/CPS/达人佣金）、活动类（满减/券/红包/坑位费）、人力类（客服/主播提成/设计）、税务类（进销项税率/视同销售）、退款类（三阶段退款率+回收率）、其他固定支出（房租/ERP/仓储）
- 每项确认：无 / 固定金额 / 百分比（基于售价/收入/利润）
- 特别确认"触发条件"：付款即收（售前回收率 0%）vs 订单成功才收（100%）
- **例外**：AI 自己试玩/实验时（用户授权自主操作）无需询问，按合理默认值建模

## 9. 数据与注意事项

- 数据 100% 在浏览器 IndexedDB，**清除浏览器数据（cookie）会清空全部数据**，建议定期备份（数据空间面板）
- 换浏览器/设备数据不跟随；无登录体系、无后端接口，**不支持 HTTP API / CLI**；AI 需通过浏览器自动化操作
- 百分数字段 UI 输入百分数（如 6），内部保存自动转小数（0.06）

## 10. 已知问题

- 收入明细表：旧版"多渠道方案"废弃残留（tbody 空）
- "添加到对比"/"查看对比"按钮：占位未完成
- 报告页顶栏参数输入框：可编辑但无事件绑定（疑似预留"试算"功能）
- 历史已修复：方案名称 placeholder（已改为"请输入方案名称"）、商品"数量"→"商品件数"、planId 不存在时显示"方案不存在，或已被移除"（不再回退演示数据）
